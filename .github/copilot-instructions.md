# Copilot Instructions

## Project Guidelines
- Use Riverbed.Quoted.App.Client.MB as the main client application, keep its main project settings/files unchanged, and migrate screens/files by wiring navigation to replace Riverbed.Pricing.Paint.Client.
- Use theme-aware text colors on the Dashboard so text remains readable in both dark and light mode; avoid hardcoded off-white-only styling.